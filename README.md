# Discord Bot

A Discord bot built with .NET.

## Getting Started

1. Clone the repository
2. Configure your Discord bot token
3. Run the bot with `dotnet run`

## Project Structure

- `src/` - Source code
- `tests/` - Unit tests
- `docs/` - Documentation
