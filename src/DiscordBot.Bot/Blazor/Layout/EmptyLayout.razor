@namespace DiscordBot.Bot.Blazor.Layout
@inherits LayoutComponentBase

@*
    Bare layout for standalone routed pages (error pages, access denied) that render
    their own full-viewport shell — the Razor Pages originals used Layout = null.
    Pages needing the admin chrome will use MainLayout when it lands (Phase E).
*@

@Body
