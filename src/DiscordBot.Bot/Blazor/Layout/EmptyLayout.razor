@inherits LayoutComponentBase

@* No chrome - the real MainLayout/GuildLayout/PortalLayout land in Phase 3 (plan §4.7). Used
   today only as the DefaultLayout for Routes.razor and by pages (like the Phase 1 smoke page)
   that don't need any shell yet. *@
@Body
