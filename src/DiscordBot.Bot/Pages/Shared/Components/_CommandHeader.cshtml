@model DiscordBot.Bot.ViewModels.Components.CommandHeaderViewModel

<div class="mb-6">
    <h1 class="page-title">@Model.Title</h1>
    @if (!string.IsNullOrEmpty(Model.Subtitle))
    {
        <p class="text-sm text-text-secondary mt-1" data-command-subtitle>@Model.Subtitle</p>
    }
</div>
